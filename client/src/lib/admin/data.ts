import type {
  CareerOpening,
  DriverApplication,
  JobApplication,
  Testimonial,
} from "./types";

/** Careers / applications / testimonials remain local until those CMS modules are wired. */
export const CAREERS: CareerOpening[] = [];
export const TESTIMONIALS: Testimonial[] = [];
export const DRIVER_APPLICATIONS: DriverApplication[] = [];
export const JOB_APPLICATIONS: JobApplication[] = [];
